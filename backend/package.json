{
  "name": "flower-coins-backend",
  "version": "1.0.0",
  "description": "FLOWER COINS Backend API with Youware Backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.0.0"
  }
}